general:
  branches:
    ignore:
      - production
machine:
  node:
    version: 6.9.1
  ruby:
    version: 2.3.0
dependencies:
  pre:
    - bundle install
    - ./scripts/prepare.sh
  cache_directories:
    - "~/.rvm"
test:
  override:
    - ./scripts/test.sh
# deployment:
#  staging:
#    branch: master
#    commands:
#      - ./scripts/deploy_staging.sh
